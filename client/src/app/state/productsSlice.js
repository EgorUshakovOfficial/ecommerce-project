import {createSlice} from '@reduxjs/toolkit';

// Initial state
let initialState = {
    products:[]
}

// Products slice
const productsSlice = createSlice({
    name:'products',
    initialState,
    reducers:{

    }
});